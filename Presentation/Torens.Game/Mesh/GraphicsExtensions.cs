namespace Torens.Game.Mesh
{
    public static class GraphicsExtensions
    {
        //public static Model CreateModel(this GraphicsDevice graphicsDevice, MeshData meshData)
        //{
        //    // https://doc.xenko.com/latest/en/manual/scripts/create-a-model-from-code.html
        //    var vertices = meshData.Vertices.Select(vertex => vertex.ToVertexNormal()).ToArray();
        //    var xenkoMeshData = new GeometricMeshData<VertexInfoExtensions>(vertices, meshData.Indices, false);

        //    var primitive = new GeometricPrimitive<VertexInfoExtensions>(graphicsDevice, xenkoMeshData);
        //    var meshDraw = primitive.ToMeshDraw();

        //    ParameterCollection parms = null;
        //    var mesh = new Xenko.Rendering.Mesh(meshDraw, parms);
        //    return new Model { mesh };
        //}
    }
}